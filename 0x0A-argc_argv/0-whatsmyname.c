#include <stdio.h>
/**
 * main - prints the program name
 * @argc: number of argument applied
 * @argv: an array of pointers to the argument
 * Return: always success
 */
int main(int __attribute__((__unused__)) argc, char *argv[])
{
	printf("%s\n", argv[0]);

	return (0);
}
