#include <stdio.h>

/**
 * main - printing all digits base 10
 *
 * Return: Always 0 (success)
 *
 */

int main(void)
{
	int num;

	for (num = 0; num <= 10; num++)
	{
		printf("%d", num);
	}

	printf("\n");

	return (0);
}
