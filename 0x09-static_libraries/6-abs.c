#include "main.h"
/**
 *  * _abs - calculet the absoulut value
 *   * @i: the integer to be compute
 *    * Return: absolut vlue of the number
 *     */
int _abs(int i)
{
		if (i < 0)
				{
							i = i * -1;
								}
			return (i);
}
